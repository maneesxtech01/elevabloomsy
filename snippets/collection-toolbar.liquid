{%- liquid
  assign enable_filtering = enable_filtering
  assign default_filter = default_filter
  assign filter_layout = filter_layout
  assign number_products_per_page = section_st.number_products_per_page
  assign sorting = section_st.sorting
  assign grid_list = section_st.grid_list
  assign products_column = products_column
-%}
{% liquid
  assign enable_rtl = settings.rtl
  assign iso_code = request.locale.iso_code
  assign lis_language_rtl = settings.language_rtl
  if lis_language_rtl != blank
    if lis_language_rtl contains iso_code
      assign enable_rtl = true
    else
      assign enable_rtl = false
    endif
  endif
%}

<!-- Desktop Toolbar (Visible only on Desktop) -->
<div class="toolbar toolbar-products toolbar-desktop hidden-md gap-30 align-center mb-20{% if filter_layout == 'horizontal' %} mt-1025-60{% endif %}" style="display: none;">
  <!-- Left Side: Results Count -->
  <div class="toolbar-left flex align-center">
    {%- if number_products_per_page -%}
      <div class="toolbar-amount relative" id="toolbar-amount">
        {%- if collection.results_count -%}
          {{ 'templates.search.results_with_count' | t: terms: collection.terms, count: collection.results_count }}
        {%- elsif collection.products_count == collection.all_products_count -%}
          {{ 'products.facets.product_count_simple' | t: count: collection.products_count }}
        {%- else -%}
          {{ 'products.facets.product_count' | t: product_count: collection.products_count, count: collection.all_products_count }}
        {%- endif -%}
      </div>
    {%- endif -%}
  </div>

  <!-- Center: Grid View Columns Switcher -->
  {%- if grid_list -%}
    <div class="show-type-products flex gap-5 justify-content-center">
      <grid-mode
        class="grid-mode pointer hover-heading-color transition w-30 h-30 content-center tooltip relative grid-mode-2{% if products_column == 2 %} active{% endif %}"
        data-grid-mode="2"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="15" height="14" fill="none">
          <g fill="currentColor"><path d="M11.073 6a3 3 0 1 0 0-6 3 3 0 0 0 0 6ZM4.07 6a3 3 0 1 0 0-6 3 3 0 0 0 0 6ZM11.073 14a3 3 0 1 0 0-6 3 3 0 0 0 0 6ZM3.927 14a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z" fill="currentColor"/></g>
        </svg>
        <span class="visually-hidden">{{ 'collections.toolbar.progress_bar.columns' | t: amount: 2 }}</span>
        <span class="tooltip-content invisible rounded-3 absolute pointer-none">{{ 'collections.toolbar.progress_bar.columns' | t: amount: 2 }}</span>
      </grid-mode>
      <grid-mode
        class="grid-mode pointer hover-heading-color transition w-30 h-30 content-center tooltip relative grid-mode-3{% if products_column == 3 %} active{% endif %}"
        data-grid-mode="3"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="15" height="14" fill="none">
          <g fill="currentColor"><path d="M2.073 4a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM2.073 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM2.073 14a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM7.073 4a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM7.073 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM7.073 14a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM12.073 4a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM12.073 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM12.073 14a2 2 0 1 0 0-4 2 2 0 0 0 0 4Z" fill="currentColor"/></g>
        </svg>
        <span class="visually-hidden">{{ 'collections.toolbar.progress_bar.columns' | t: amount: 3 }}</span>
        <span class="tooltip-content invisible rounded-3 absolute pointer-none">{{ 'collections.toolbar.progress_bar.columns' | t: amount: 3 }}</span>
      </grid-mode>
      <grid-mode
        class="grid-mode pointer hover-heading-color transition w-30 h-30 content-center tooltip relative grid-mode-4{% if products_column == 4 %} active{% endif %}"
        data-grid-mode="4"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="14" fill="none">
          <g fill="currentColor"><path d="M2.073 4a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM2.073 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM2.073 14a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM7.073 4a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM7.073 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM7.073 14a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM12.073 4a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM12.073 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM12.073 14a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM17.073 4a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM17.073 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM17.073 14a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM17.073 4a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM17.073 9a2 2 0 1 0 0-4 2 2 0 0 0 0 4ZM17.073 14a2 2 0 1 0 0-4 2 2 0 0 0 0 4Z" fill="currentColor"/></g>
        </svg>
        <span class="visually-hidden">{{ 'collections.toolbar.progress_bar.columns' | t: amount: 4 }}</span>
        <span class="tooltip-content invisible rounded-3 absolute pointer-none">{{ 'collections.toolbar.progress_bar.columns' | t: amount: 4 }}</span>
      </grid-mode>
    </div>
  {%- endif -%}

  <!-- Right Side: Sort By Dropdown -->
  {%- if sorting -%}
    <div class="toolbar-sorter sorter flex gap-10 align-center relative">
      <div class="select-custom pointer user-select-none">
        {%- assign sort_by = collection.sort_by | default: collection.default_sort_by -%}
        {%- liquid
          assign current_sort_name = 'Best Selling'
          case sort_by
            when 'best-selling'
              assign current_sort_name = 'Best Selling'
            when 'created-descending'
              assign current_sort_name = 'Newest'
            when 'title-ascending'
              assign current_sort_name = 'A-Z'
            when 'title-descending'
              assign current_sort_name = 'Z-A'
            when 'price-ascending'
              assign current_sort_name = 'Price (low to high)'
            when 'price-descending'
              assign current_sort_name = 'Price (high to low)'
            else
              for option in collection.sort_options
                if option.value == sort_by
                  assign current_sort_name = option.name
                endif
              endfor
          endcase
        -%}
        <select-sorter class="select-selected user-select-none inline-flex align-center gap-10 gap-lg-20 lh-normal pointer overlay">
          <div class="flex flex-column align-start">
            <span class="sort-by-label">{{- 'products.facets.sort_by_label' | t -}}</span>
            <span class="sort-by-active heading-color subheading_weight">
              {{ current_sort_name }}
            </span>
          </div>
          <svg class="icon-down transition active-rotated" width="10" height="6">
            <use href="#icon-arrow-down"></use>
          </svg>
        </select-sorter>
        <div
          class="select-custom__content facet-filters__sort invisible transition rounded-5 select__select absolute shadow list-none z-2 py-20 px-25 color-default{% if enable_rtl %} left-0{% else %} right-0{% endif %}"
          id="SortBy"
          aria-describedby="a11y-refresh-page-message"
        >
          <icon-close class="pointer-none border bg-white inline-flex hidden-sm mx-auto rounded-50 overflow-hidden absolute top-0 inset-x-0 w-50 h-50 content-center heading-color">
            <svg width="13" height="13" viewBox="0 0 13 13" fill="none" class="transition">
              <use href="#icon-close"></use>
            </svg>
          </icon-close>
          <div class="h-full overflow-auto sort-by-radio-list">
            {%- assign sort_options = "best-selling|Best Selling,created-descending|Newest,title-ascending|A-Z,title-descending|Z-A,price-ascending|Price (low to high),price-descending|Price (high to low)" | split: ',' -%}
            {%- for option in sort_options -%}
              {%- assign option_parts = option | split: '|' -%}
              {%- assign val = option_parts[0] -%}
              {%- assign label = option_parts[1] -%}
              
              <filter-sort
                class="filter-sort-option flex align-center gap-10 py-8 pointer text-color hover-heading-color transition body_weight"
                value="{{ val }}"
                {% if val == sort_by %}
                  selected="selected"
                {% endif %}
              >
                <span class="custom-radio-button inline-flex content-center rounded-50 border relative {% if val == sort_by %}checked{% endif %}" style="width: 18px; height: 18px; flex-shrink: 0;">
                  <span class="custom-radio-dot absolute rounded-50" style="width: 8px; height: 8px; top: 4px; left: 4px; display: {% if val == sort_by %}block{% else %}none{% endif %};"></span>
                </span>
                <span>{{ label }}</span>
              </filter-sort>
            {%- endfor -%}
          </div>
        </div>
      </div>
    </div>
  {%- endif -%}
</div>

<!-- Mobile Toolbar (Visible only on Mobile) -->
<div class="toolbar toolbar-products toolbar-mobile hidden block-md align-center justify-content-between mb-20" style="display: flex;">
  <!-- Left Side: Filter button -->
  {%- if enable_filtering -%}
    <btn-filter
      class="btn-filter border flex gap-10 align-center px-15 py-10 pointer rounded-5 heading-style transition mr-15"
      data-filter-layout="{{ filter_layout }}"
    >
      <svg width="14" height="15" viewBox="0 0 14 15" fill="none">
        <path d="M12.8333 2.16666H1.16663L5.83329 7.67499V11.5L8.16663 12.6667V7.67499L12.8333 2.16666Z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      {{ 'collections.toolbar.filter' | t }}
    </btn-filter>
  {%- endif -%}

  <!-- Right Side: 1 Column / 2 Column switcher -->
  {%- if grid_list -%}
    <div class="mobile-view-switcher flex gap-10 align-center" style="margin-left: auto;">
      <!-- 2 Columns -->
      <mobile-column-switch
        class="grid-mode pointer tooltip relative w-30 h-30 inline-flex content-center"
        data-columns="2"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="14" fill="none" viewBox="0 0 18 14">
          <rect width="6" height="12" x="1" y="1" stroke="currentColor" stroke-width="2" rx="1"/>
          <rect width="6" height="12" x="11" y="1" stroke="currentColor" stroke-width="2" rx="1"/>
        </svg>
        <span class="visually-hidden">2 columns</span>
      </mobile-column-switch>
      <!-- 1 Column (Default) -->
      <mobile-column-switch
        class="grid-mode pointer tooltip relative w-30 h-30 inline-flex content-center active"
        data-columns="1"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="none" viewBox="0 0 14 14">
          <rect width="12" height="12" x="1" y="1" stroke="currentColor" stroke-width="2" rx="1"/>
        </svg>
        <span class="visually-hidden">1 column</span>
      </mobile-column-switch>
    </div>
  {%- endif -%}
</div>
